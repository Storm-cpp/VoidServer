#ifndef MESSAGE_H
#define MESSAGE_H

#include <string>

// Structure for a message
struct Message {
    std::string content;
    std::string sender;
    // Add more fields as needed, e.g., timestamp, channel
};

#endif // MESSAGE_H